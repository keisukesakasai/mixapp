apiVersion: apps/v1
kind: Deployment
metadata:
  name: investor-agent
  namespace: mixapp
  labels:
    app: investor-agent
spec:
  replicas: 1
  selector:
    matchLabels:
      app: investor-agent
  template:
    metadata:
      labels:
        app: investor-agent
    spec:
      containers:
        - name: investor-agent
          image: ghcr.io/keisukesakasai/mixapp-investor-agent:latest
          ports:
            - containerPort: 8000
          env:
            - name: OPENAI_API_KEY
              valueFrom:
                secretKeyRef:
                  name: investor-agent-openai
                  key: openai-api-key
            - name: MODEL
              value: "gpt-4o-mini"
            - name: REDIS_URL
              value: "redis://redis:6379/0"
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 10
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: investor-agent
  namespace: mixapp
  labels:
    app: investor-agent
spec:
  selector:
    app: investor-agent
  ports:
    - port: 8000
      targetPort: 8000
      name: http
