# API キーなど Git に含めたくない値（このファイルは .gitignore で無視される）
# 使い方: cp .env.example .env して .env に実際の値を入れる。
# リポジトリルートに .env を置くと Investor Agent のローカル実行時に自動で読まれる。

OPENAI_API_KEY=

# Datadog（APM / LLM Observability）。K8s では Secret datadog-secret から渡す。ローカルで観測する場合のみ設定。
DD_API_KEY=
